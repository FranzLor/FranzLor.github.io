---
title: Franz Lor Portfolio
layout: 'base.njk'

---

<section id="about">
    <h2>HEY THERE, I'M FRANZ</h2>
    <h3><strong>I'm a programmer.</strong></h3><br>
    <p>
        I have a passion for games—whether it's playing or learning all about them.<br>
        With this passion comes my motivation to develop<br>
        games using C++, C#, or with game engines.<br><br>
        <strong>Check them out here</strong>!
    </p>
    <div class="button-container">
        <a href="#projects" class="btn">PROJECTS</a>
    </div>

</section>

<section id="projects">
    <h2>PROJECTS</h2>
    <div class="project-intro">
        <p>
            Check out the projects I've worked on.<br>
            Each one carrying a unique learning experience.<br>
            <strong>Click on a project to learn more about it</strong>.<br>
        </p>
    </div>

    <div class="projects-grid">
        {% for project in collections.projects %}
            <div class="project">
                <div class="project-tile">
                    <img
                            src="{{ project.logo }}"
                            alt="{{ project.alt }}"
                            width="{{ project.width }}"
                            height="{{ project.height }}"
                    >
                    <div class="project-overlay">
                        <h3>{{ project.title }}</h3>
                        <p>{{ project.subtitle }}</p>
                        <a href="{{ project.url }}">View Project</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

    <div class="project-conclusion">
        <p>
            I'll be showcasing more projects in the future.<br>
            Meanwhile, check out the rest of my projects on my GitHub.
        </p>
        <div class="button-container">
            <a href="https://github.com/FranzLor?tab=repositories" class="btn">View All Projects</a>
        </div>
    </div>
</section>

<section id="devblog">
    <h2>DEVBLOG</h2>
    <div class="devblog-intro">
        <p>
            Dive into my development journey with tutorials and insights.<br>
            These articles cover different aspects of game development<br>
            such as coding patterns and game design.<br>
            <strong>Click on a devblog to read more about it.</strong>
        </p>
    </div>

    <div class="devblog-grid">
        {% for post in collections.devblog %}
            <div class="devblog-post">
                <div class="devblog-tile">
                    <img
                            src="{{ post.thumbnail }}"
                            alt="{{ post.alt }}"
                            width="300"
                            height="200"
                    >
                    <div class="devblog-overlay">
                        <h3>{{ post.title }}</h3>
                        <p>{{ post.subtitle }}</p>
                        <span class="devblog-date">{{ post.formattedDate }}</span>
                        <a href="{{ post.url }}">Read Article</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</section>

<section id="contact">
    <div class="contact-main">
        <h2>CONTACT</h2>
        <p>
            For career opportunities, game dev collaborations, and questions.<br>
            Feel free to contact me anytime and I'll get back to you.<br>
        </p>
        <div class="contact-form-container">
            <form onsubmit="return sendMail();">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" required>

                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>

                <label for="subject">Subject</label>
                <input type="text" id="subject" name="subject" required>

                <label for="message">Message</label>
                <textarea id="message" name="message" rows="4" required></textarea>

                <button type="submit" id="button"></button>
            </form>
        </div>
    </div>
</section>


<!-- Mobile / Tablet Project Tile -->
<script>
    // Add click event listener to all project tiles
    document.querySelectorAll('.project-tile').forEach(tile => {
        tile.addEventListener('click', function (e) {
            // Remove 'active' class from other tiles
            document.querySelectorAll('.project-tile.active').forEach(activeTile => {
                if (activeTile !== tile) {
                    activeTile.classList.remove('active');
                }
            });

            // Toggle 'active' class on the clicked tile
            this.classList.toggle('active');
        });
    });
</script>


<!-- Contact Form Button Animation -->
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const button = document.getElementById("button");
        const form = document.querySelector("form");

        if (button && form) {
            button.addEventListener("click", function(event) {
                if (form.checkValidity()) {
                    // Button animation
                    button.classList.add("onclic");

                    setTimeout(function() {
                        button.classList.remove("onclic");
                        button.classList.add("validate");

                        setTimeout(function() {
                            button.classList.remove("validate");
                        }, 1250);
                    }, 2250);
                } else {
                    form.querySelector(":invalid").focus();
                }
            });
        }
    });
</script>

<!-- Smooth Scrolling from Clicks Script -->
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const smoothScrollLinks = document.querySelectorAll('a[href^="#"]');
        smoothScrollLinks.forEach((link) => {
            link.addEventListener("click", function (e) {
                e.preventDefault();

                const targetId = this.getAttribute("href").substring(1);
                const targetElement = document.getElementById(targetId);

                targetElement.scrollIntoView({
                    behavior: "smooth",
                });
            });
        });
    });
</script>